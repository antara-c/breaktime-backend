spring:
  application:
    name: break-time
  jpa:
    hibernate:
      ddl-auto: update  
    show-sql: true
  datasource:
    username: root
    password: 
    url: jdbc:mysql://localhost:3306/breaktime
    driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 8080